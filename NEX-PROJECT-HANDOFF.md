# راهنمای ادامه پروژه Nex — برای Claude Code دسکتاپ

این فایل رو در ابتدای سشن جدید در Claude Code دسکتاپ به Claude بده (یا لینکش کن) تا context کامل داشته باشه. تاریخ نوشتن: 2026-08-13.

## مخزن و برنچ

- ریپو: `sanyzrn/DbsNex` (GitHub)
- برنچ کاری فعلی: `claude/project-status-review-jqgb5i`
- آخرین کامیت پوش‌شده روی این برنچ: `9abfde9`
- روند کار در این پروژه: کدها رو من (Claude) روی این برنچ کامیت و پوش می‌کنم، PR رو **خودت دستی** می‌سازی/مرج می‌کنی (تا الان PR شماره‌دار — آخری که مرج شده #104 بوده، main الان سبزه).
- الان روی این برنچ یه PR باز نیست — کامیت `9abfde9` پوش شده ولی هنوز PR نساختم (چون نگفته بودی بسازم).

## همین الان چی انجام شد (این سشن)

یه سند ممیزی امنیتی/فنی به اسم `Nex-Audit-Full-Export.md` با ۲۷ تا finding (به اسم NEX-01 تا NEX-27) روی sync client و backend وجود داشت. اکثرش (Group A تا D و بیشتر E/F) قبلاً توی کامیت‌های قبلی حل شده بود. من چک کردم و دو مورد باقی‌مونده رو بستم:

1. **NEX-15 (نصفه‌ی باقی‌مونده):** توکن‌های دستگاه (device bearer token) هیچ‌وقت expire نمی‌شدن — فقط با revoke دستی باطل می‌شدن. الان:
   - migration جدید: `apps/backend/src/db/migrations/0007_device_token_expiry.sql` — ستون `expires_at` به جدول `devices` اضافه شد.
   - env var جدید: `DEVICE_TOKEN_TTL_DAYS` (پیش‌فرض ۳۶۵ روز).
   - `requireDevice` توکن expire شده رو رد می‌کنه؛ دستگاه‌های قدیمی (قبل از این migration) مقدار `expires_at = NULL` دارن یعنی هیچ‌وقت expire نمی‌شن (backward-compatible، کسی logout نشد).
   - تابع مرده `safeEqualHex` (صفر call site) حذف شد.

2. **باگ جدید که موقع تست پیدا شد:** `SyncClient.sync()` توی `packages/data/lib/sync_client/sync_client.dart` کل outbox رو توی یه request می‌فرستاد، ولی سرور آرایه‌ی `notes`/`tags` رو حداکثر ۵۰۰ آیتم قبول می‌کنه. یعنی هر دستگاهی که outbox بزرگ‌تر از ۵۰۰ داشت (مثلاً بعد از یه دوره‌ی طولانی آفلاین بودن) اصلاً نمی‌تونست sync کنه — push با 400 fail می‌شد و حتی pull هم اجرا نمی‌شد. الان push به batch های ۵۰۰تایی تقسیم می‌شه و loop می‌زنه.

هر دو مورد با یه Postgres محلی و اجرای واقعی سرور تست شدن (نه فقط unit test) — همه‌ی ۱۳ تا کیس `sync_matrix_test.dart` روی سرور واقعی pass شدن.

### چیزهایی که از audit عمداً باز مونده (تصمیم قبلی، نه فراموشی)

- **NEX-24** (تبدیل به Dart pub workspace با یه lockfile مشترک) — نیاز به برنچ جدا داره چون می‌تونه ورژن‌های resolve‌شده رو جابه‌جا کنه.
- **بقیه‌ی Group E** (pool sizing، Redis برای rate limit مشترک بین چند replica، health probe caching، purge با advisory lock و فیلتر دستگاه‌های revoke‌شده) — این‌ها فقط وقتی backend واقعاً public/deployed باشه ارزش داره. الان `apps/backend` فقط local/CI اجرا می‌شه.

## موضوع اصلی و بزرگ باز: هوش مصنوعی آفلاین (Local/Offline AI)

این مهم‌ترین موضوعیه که هنوز شروع نشده و باید ادامه‌ش بدیم.

### وضعیت فعلی (Phase 0 — فقط یه harness آزمایشی)

- سند راهبردی موجود: `docs/09-ai.md` — بخش **"Offline Local-Model Track (Phase 0)"** توش هست. قانون کلی و همیشگی پروژه: *"AI is optional. AI never slows down capture."*
- تصمیم‌های گرفته‌شده تا الان:
  - runtime انتخابی: پکیج Dart `llama_cpp_dart`, pin شده روی ورژن دقیق `0.9.0-dev.10` توی `packages/ai/pubspec.yaml`.
  - جای کد: توی `packages/ai` (نه `packages/core`) — یه CI job به اسم `ai-deletion-proof` تضمین می‌کنه که حذف کامل `packages/ai` باعث نشکستن بقیه‌ی build نشه.
  - محدودیت: فقط arm64-v8a (بدون x86_64 یا 32-bit).
  - هدف توزیع این مسیر: Cafe Bazaar / Myket (نه Google Play — اون یه تصمیم جدای بعدیه).
- **پوشه‌ی harness:** `experiments/local_ai_bench/` — یه پروژه‌ی Flutter کاملاً مستقل (خارج از `apps/client` و `packages/`)، چون یه تلاش قبلی (PR #84) که این تست رو مستقیم توی `apps/client` گذاشته بود، CI رو شکونده بود (نقض قانون deletion-proof).
  - `experiments/local_ai_bench/README.md` — توضیح کامل نحوه‌ی اجرا.
  - `experiments/local_ai_bench/android/app/libs/README.md` — توضیح گرفتن native prebuilt.

### تست دستی‌ای که هنوز مونده (تو باید انجامش بدی، من نمی‌تونم)

من (Claude، توی این sandbox) نه به GitHub خارج از این ریپو دسترسی دارم نه به Hugging Face، و نه گوشی اندروید واقعی دارم. پس این مرحله همیشه دست خودته:

1. فایل `llama-cpp-dart.aar` رو از releases ریپوی `github.com/netdur/llama_cpp_dart` دانلود کن (نسخه‌ای که با pin شده‌ی `0.9.0-dev.10` می‌خونه — قبل از دانلود چک کن که pin عوض نشده باشه) و بذارش توی `experiments/local_ai_bench/android/app/libs/llama-cpp-dart.aar` (نسخه CPU+mtmd arm64-v8a، نه نسخه‌ی `-hexagon.aar`).
2. یه مدل GGUF کوچیک (۱ تا ۴ میلیارد پارامتر، quantized حدود Q4_K_M یا Q4_0، instruct-tuned) از Hugging Face دانلود کن. توی README چند خانواده اسم برده شده (Gemma 3 1B/4B، Qwen2.5 3B، Phi-3.5-mini) ولی این‌ها verify نشدن — خودت موجود بودنشون رو چک کن.
3. با `adb push` مدل رو روی گوشی بذار، harness رو با `flutter run -d <device-id>` **روی یه گوشی اندروید واقعی (نه امولاتور)** اجرا کن.
4. چند بار پشت سر هم اجرا کن و این‌ها رو یادداشت کن:
   - tokens/sec
   - time-to-first-token
   - آیا گوشی بعد از چند اجرای پشت‌سرهم داغ می‌کنه و tok/s افت می‌کنه (throttling)؟
   - مدل گوشی

این عددها تصمیم واقعی go/no-go برای Phase 2 رو می‌سازن.

### چشم‌انداز کامل محصول (خلاصه‌ی چیزی که قبلاً بهم گفتی، برای یادآوری)

هنوز هیچ‌کدوم از این‌ها پیاده‌سازی نشده — فقط تصمیم/طراحی گرفته نشده. این‌ها رو باید یه‌جا (احتمالاً به‌روزرسانی `docs/09-ai.md` با فازهای واقعی Phase 1/2/3+) رسمی کنیم:

- **مدل محلی قابل انتخاب/دانلود توسط کاربر**، بدون وابستگی به API یا اینترنت.
- **چت‌بات همه‌کاره** — نه محدود به یه موضوع خاص، بلکه محدود به یه سقف طول/پیچیدگی پاسخ. سوال عمومی، ریاضی، کمک نوشتن/نامه، سوال اکسل، خلاصه‌سازی، ترجمه، ایده‌پردازی و... . اگر درخواست از سقف رد بشه (مثلاً "یه نرم‌افزار کامل بساز")، AI باید صریح بگه این خارج از scope فعلی Nex هست — نه اینکه پاسخ ناقص یا خیلی طولانی بده.
- **یکپارچگی عمیق با قابلیت‌های داخلی Nex** — AI باید بتونه تشخیص بده و اجرا کنه: ساخت/ویرایش نوت، ساخت task/reminder، دسته‌بندی، جستجو توی محتوای ذخیره‌شده، مدیریت اطلاعات شخصی.
- **پروفایل شخصی و حافظه‌ی قابل کنترل** — با اجازه‌ی کاربر، به‌تدریج اطلاعات/ترجیحات/الگوهای رفتاری کاربر رو یاد بگیره تا پاسخ‌ها شخصی‌سازی بشه. مستقل از مدل، ذخیره‌ی محلی.
- **انتقال context از AI های دیگه** — Nex یه پرامپت آماده به کاربر می‌ده، کاربر می‌بردش توی یه AI دیگه (مثلاً ChatGPT) که مدت‌هاست استفاده می‌کنه، جواب رو برمی‌گردونه توی Nex، و Nex تحلیل/ساختاردهی می‌کنه و با تایید کاربر — با امکان دیدن/ویرایش/انتخاب/حذف هر بخش — توی بخش‌های مشخص حافظه ذخیره می‌شه (پروفایل، ترجیحات، علایق، سبک ارتباطی، عادت‌ها، context کاری، حافظه‌ی بلندمدت). کاملاً شفاف و کنترل‌شده توسط کاربر.
- **تقسیم‌بندی پولی/رایگان:**
  - رایگان برای همه (متناسب با مدل/سخت‌افزار انتخابی): چت عمومی، سوال‌وجواب، کمک نوشتن، ترجمه، خلاصه‌سازی، ایده‌پردازی.
  - پولی/اشتراکی (فقط قابلیت‌های پیشرفته‌ی "دستیار شخصی"): حافظه‌ی بلندمدت/شخصی، یادگیری تدریجی کاربر، پروفایل هوشمند، یادگیری الگوی رفتاری، اجرای task/reminder، یکپارچگی عمیق با ابزارهای داخلی، انتقال context از AI های دیگه.
  - زیرساخت فنی باید از همون اول ماژولار/قابل‌توسعه باشه (پلن‌ها، سطوح دسترسی، فعال/غیرفعال کردن قابلیت‌ها) — ولی سیستم پرداخت/اشتراک کامل لازم نیست همین الان پیاده بشه، فقط زیرساخت گیت‌بندی باید آماده باشه.
- **مهم:** قابلیت‌های آنلاین فعلی AI (تبدیل صدا به متن، استخراج متن از عکس/OCR، خلاصه‌سازی، پیشنهاد تگ) باید بعد از اومدن AI محلی، **کاملاً آفلاین هم کار کنن** (متناسب با مدل/سخت‌افزار انتخابی) و **پولی نشن**. فقط قابلیت‌های جدید/پیشرفته (دستیار شخصی) قراره در آینده پولی بشن.

### قدم بعدی پیشنهادی (هنوز با کاربر نهایی نشده)

قبل از این سشن، پیشنهاد من این بود: چون این ویژگی خیلی بزرگه و تصمیم‌های معماری زیادی داره (memory schema، tool-calling contract، هوک‌های gating رایگان/پولی)، بخش زیادی از طراحی **مستقل از نتیجه‌ی بنچمارک واقعیه** و می‌تونه همین الان (بدون نتیجه‌ی تست گوشی) شروع بشه:
- رسمی کردن فازهای Phase 1/2/3+ توی `docs/09-ai.md` (الان فقط نوشته "still being phased").
- طراحی قرارداد tool-calling برای اینکه AI بتونه قابلیت‌های داخلی Nex رو صدا بزنه.
- طراحی schema حافظه/پروفایل محلی.
- طراحی فلوی انتقال context از AI های دیگه.
- طراحی هوک‌های ماژولار برای gating رایگان/پولی (بدون پیاده‌سازی کامل پرداخت).

این پیشنهاد هنوز باهات نهایی نشده بود — موضوع audit وسط اومد. باید این گفتگو رو با Claude Code دسکتاپ ادامه بدیم.

## کارهای باز دیگه (کوچیک‌تر)

- **ترجمه فارسی:** منتظر مثال‌های مشخص از کیفیت ترجمه فارسی هستم که قول داده بودی بفرستی (هنوز نیومده).
- **جستجوی هوشمند با AI:** یه آیتم باز هست به اسم "Scope AI-integrated search feature" — احتمالاً با موضوع "جستجو توی محتوای ذخیره‌شده" که توی چشم‌انداز AI آفلاین بالا هم اومده هم‌پوشونی داره. باید مشخص بشه این زیرمجموعه‌ی همون کاره یا جدا.
- **بازطراحی صفحه‌ی Settings:** منتظر فایل Figma جدید از طرف توئه — تا نرسه دست نمی‌زنیم.
- **مهاجرت به اکانت گیت‌هاب دیگه (`hanytarek143-sketch`):** قبلاً تلاش شد ولی به یه محدودیت معماری خورد (نمی‌شه یه سشن رو به یه ریپوی صاحب متفاوت وصل کرد مگر از اول با اون ریپو شروع بشه) و نیاز به authorize کردن Claude GitHub App روی اکانت جدید داشت که کامل نشد. خودت گفتی مهم نیست، فعلاً ادامه بدیم. **دست نزن مگر خودم دوباره مطرحش کنم.**

## محدودیت‌های محیطی مهم

- **سهمیه‌ی GitHub Actions minutes** (نه storage — اون قبلاً حل شده) تموم شده بود (۲۰۰۰/۲۰۰۰)، ریست حدود اول سپتامبر ۲۰۲۶. چون از ایران امکان پرداخت نداری، هیچ راه‌حل پولی/GitHub-billing پیشنهاد نکن — این یه محدودیت دائمیه تا خودت چیزی بگی.
- برای اجرای local کارهای Dart/Flutter، SDK توی مسیر `/opt/flutter-sdk/flutter/bin` (نسخه‌ی pin‌شده در `.fvmrc`: Flutter 3.35.5 / Dart 3.9.x) — ممکنه روی سیستم خودت مسیر فرق کنه، از `fvm` استفاده کن.

## چطور شروع کنیم توی Claude Code دسکتاپ

1. ریپو `sanyzrn/DbsNex` رو کلون کن، برنچ `claude/project-status-review-jqgb5i` رو checkout کن (یا اگه مرج کردی، از `main` شروع کن).
2. این فایل رو به Claude بده (یا بذارش یه‌جا قابل‌خوندن توی پروژه) تا context کامل داشته باشه.
3. بگو کدوم بخش رو ادامه بدیم — پیشنهاد من: یا (الف) خودت اول تست گوشی واقعی رو انجام بده و نتیجه رو بیار، یا (ب) بدون منتظر موندن، بریم سراغ طراحی مستقل‌از-بنچمارک (فازبندی `docs/09-ai.md` + طراحی tool-calling/memory/cross-AI-import).
