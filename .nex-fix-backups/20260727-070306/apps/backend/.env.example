# Nex backend — local development configuration.
# Copy to `.env`. No secrets are committed.

PORT=4000

# Local PostgreSQL instance (Phase 0: connection is opened lazily and the API
# stays up even if the database is unreachable — no route depends on it yet).
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/nex
