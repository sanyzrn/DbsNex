<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="LaunchTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowSplashScreenBackground">@color/nex_launch_light</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/nex_splash_icon_light</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/nex_launch_light</item>
        <item name="android:windowActionModeOverlay">true</item>
    </style>
    <style name="NormalTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/nex_launch_light</item>
        <item name="android:fontFamily">sans</item>
    </style>
    <!--
        The share window. Translucent, with no starting window and no splash:
        a share should leave you where you were, and the only thing it puts on
        screen is a toast once the note exists. See ShareActivity.
    -->
    <style name="ShareTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowSplashScreenBackground">@android:color/transparent</item>
    </style>
</resources>
