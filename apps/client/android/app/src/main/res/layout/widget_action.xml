<?xml version="1.0" encoding="utf-8"?>
<!-- One-tap capture widgets (quick capture, voice memo, camera) share this
     layout; the provider sets the icon, the disc fill and both labels in
     code so one layout serves three widgets and both themes. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layoutDirection="locale"
    android:background="@drawable/widget_card_bg_light"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="16dp">

    <FrameLayout
        android:id="@+id/widget_disc"
        android:layout_width="52dp"
        android:layout_height="52dp">

        <ImageView
            android:id="@+id/widget_disc_bg"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:src="@drawable/widget_disc_accent_light" />

        <ImageView
            android:id="@+id/widget_disc_icon"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:layout_gravity="center"
            android:contentDescription="@null"
            android:src="@drawable/ic_widget_add" />
    </FrameLayout>

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:ellipsize="end"
        android:fontFamily="sans"
        android:gravity="center"
        android:maxLines="1"
        android:text="Capture"
        android:textColor="#FF262626"
        android:textSize="16sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/widget_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:ellipsize="end"
        android:fontFamily="sans"
        android:gravity="center"
        android:maxLines="1"
        android:text="Tap to capture a note"
        android:textColor="#FF5C5C5C"
        android:textSize="12sp" />
</LinearLayout>
