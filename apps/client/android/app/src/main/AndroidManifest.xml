<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!--
      Lets the in-app updater hand a downloaded APK to the system installer.
      This is not a silent-install permission: Android still shows its own
      install prompt, and the user must additionally allow Nex to install
      unknown apps once, in device settings. Nex is distributed outside Play,
      so there is no store to do this for it.
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!--
      Android 11 package visibility. Opening a note's file with the device's
      default handler resolves the receiving activity first, and without this
      declaration that lookup comes back empty on API 30+ — so every "Open"
      would report "no app can open this file" even with a viewer installed.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="*/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="*/*" />
        </intent>
    </queries>

    <application
        android:label="Nex"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:enableOnBackInvokedCallback="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data android:name="io.flutter.embedding.android.NormalTheme" android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.sanyzrn.nex.TEXT_CAPTURE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>
        <!-- Four widgets, one provider class each, so the launcher's widget
             picker offers them as four distinct entries with no configuration
             step. All four share NexWidgetProvider's rendering and read the
             snapshot file the Flutter side keeps fresh. -->
        <receiver android:name=".NexQuickCaptureWidget" android:exported="true" android:label="@string/widget_quick_capture_label">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_info_quick_capture" />
        </receiver>
        <receiver android:name=".NexVoiceWidget" android:exported="true" android:label="@string/widget_voice_label">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_info_voice" />
        </receiver>
        <receiver android:name=".NexCameraWidget" android:exported="true" android:label="@string/widget_camera_label">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_info_camera" />
        </receiver>
        <receiver android:name=".NexNotesWidget" android:exported="true" android:label="@string/widget_notes_label">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_info_notes" />
        </receiver>
        <service
            android:name=".NexWidgetNotesService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />
        <meta-data android:name="flutterEmbedding" android:value="2" />
    </application>
</manifest>