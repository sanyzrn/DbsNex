name: nex_ai
description: >-
  Nex optional AI adapters (v3+). Optional leaf package — nothing in the
  capture path may import it.
publish_to: "none"
version: 0.0.1

environment:
  sdk: ^3.5.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
