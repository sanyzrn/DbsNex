name: nex_ai
description: >-
  Nex optional AI leaf (v3+). Re-exports Core AI contracts/adapters. Deleting
  this package must leave core/data/client compiling (see docs/09-ai.md).
publish_to: "none"
version: 0.1.0

resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  nex_core:
    path: ../core

dev_dependencies:
  lints: ^5.0.0
  nex_data:
    path: ../data
  test: ^1.25.0
