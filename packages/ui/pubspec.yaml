name: nex_ui
description: >-
  Nex shared Flutter widget / design-system package. Depends on Flutter by
  design; never imported by packages/core or packages/data.
publish_to: "none"
version: 0.0.1

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
