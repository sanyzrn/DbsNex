/// Nex — Shared Flutter widget / design-system package.
///
/// Phase 0 scaffold: contains **no widgets and no tokens yet**. The Light/Dark
/// token pairs and Comfort Mode axis from `05-design.md` are implemented in
/// Phase 1.7 under `lib/tokens/`; shared widgets land under `lib/widgets/`.
library nex_ui;

/// Package identity. Used by the Phase 0 scaffold test.
const String packageName = 'nex_ui';
